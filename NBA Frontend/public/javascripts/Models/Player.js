class Player {
  constructor(playerID, firstName, lastName, position, team) {
    this.playerID = playerID;
    this.firstName = firstName;
    this.lastName = lastName;
    this.position = position;
    this.team=team;
  }
}

module.exports = Player;
